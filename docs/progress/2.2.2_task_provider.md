# 2.2.2 - Crear Provider Global de Tareas

**Estado:** ✅ Completada  
**Fecha:** 13 de noviembre de 2025  
**Dependencias:** 2.2.1

---

## 📋 Descripción

Creación del provider global para gestión de estado de tareas usando Riverpod. Este provider implementa la capa de presentación que conectará las pantallas con la lógica de negocio (use cases) y repositorios.

---

## ✅ Tareas Completadas

### 1. Creación de TaskListState
- ✅ Definida clase inmutable con propiedades: `tasks`, `isLoading`, `error`
- ✅ Implementado método `copyWith()` para actualizaciones inmutables
- ✅ Documentación completa con DartDoc

### 2. Implementación de TaskListNotifier
- ✅ Extendido de `StateNotifier<TaskListState>`
- ✅ Métodos CRUD implementados:
  - `loadTasks()` - Cargar todas las tareas
  - `createTask()` - Crear nueva tarea
  - `updateTask()` - Actualizar tarea existente
  - `deleteTask()` - Eliminar tarea
  - `archiveTask()` - Archivar tarea (soft delete)
- ✅ Método `clearError()` para manejo de errores
- ✅ Estructura preparada con TODOs para integración futura

### 3. Provider Global
- ✅ Creado `taskListProvider` usando `StateNotifierProvider`
- ✅ Documentación con ejemplos de uso
- ✅ Estructura lista para inyección de dependencias

---

## 📁 Archivos Creados

- `lib/presentation/providers/task_provider.dart` - Provider principal de tareas con 217 líneas

---

## 🔧 Archivos Modificados

- `docs/TASK_BREAKDOWN.md` - Actualizado estado de tarea 2.2.2

---

## 🧪 Validaciones

```bash
flutter analyze  # ✅ No issues found!
dart format      # ✅ 0 changed (código formateado)
```

---

## 📝 Decisiones Técnicas

### 1. Estructura Placeholder con TODOs
**Decisión:** Implementar provider con estructura completa pero lógica placeholder.

**Razón:** 
- Permite tener arquitectura de presentación lista
- No bloquea desarrollo de otras capas
- TODOs bien documentados indican qué completar en Sprint 2
- Facilita testing incremental

### 2. StateNotifier vs Notifier
**Decisión:** Usar `StateNotifier<TaskListState>` en lugar de `Notifier`.

**Razón:**
- StateNotifier es más simple para casos de uso CRUD básicos
- Mejor performance al evitar rebuilds innecesarios
- Estado inmutable con `copyWith()` previene bugs
- Patrón estándar en Riverpod para gestión de listas

### 3. Separación de Estado y Lógica
**Decisión:** Crear `TaskListState` separado de `TaskListNotifier`.

**Razón:**
- Facilita testing (estado es POJO testeable)
- Permite compartir estado entre múltiples notifiers si necesario
- Cumple con Single Responsibility Principle
- Estado serializable para debugging

### 4. Manejo de Errores en Estado
**Decisión:** Incluir `error: String?` en el estado.

**Razón:**
- UI puede mostrar errores de forma reactiva
- Evita excepciones no manejadas que crashean la app
- Usuario siempre informado del estado de operaciones
- Método `clearError()` permite limpiar errores después de mostrarlos

---

## 🏗️ Arquitectura Implementada

```
┌─────────────────────────────────────────┐
│         TaskListScreen (UI)             │
│  - Observa taskListProvider             │
│  - Muestra loading/error/data           │
└─────────────────┬───────────────────────┘
                  │ ref.watch / ref.read
                  ↓
┌─────────────────────────────────────────┐
│      taskListProvider                   │
│  StateNotifierProvider<                 │
│    TaskListNotifier,                    │
│    TaskListState                        │
│  >                                      │
└─────────────────┬───────────────────────┘
                  │
                  ↓
┌─────────────────────────────────────────┐
│      TaskListNotifier                   │
│  - loadTasks()                          │
│  - createTask()                         │
│  - updateTask()                         │
│  - deleteTask()                         │
│  - archiveTask()                        │
└─────────────────┬───────────────────────┘
                  │ (Futuro: Sprint 2)
                  ↓
┌─────────────────────────────────────────┐
│      TaskRepository (Interface)         │
│  - getAll()                             │
│  - create()                             │
│  - update()                             │
│  - delete()                             │
└─────────────────────────────────────────┘
```

---

## 📖 Ejemplo de Uso

```dart
// En un StatelessWidget consumiendo el provider
class TaskListScreen extends ConsumerWidget {
  @override
  Widget build(BuildContext context, WidgetRef ref) {
    // Observar estado
    final taskState = ref.watch(taskListProvider);
    
    // Mostrar loading
    if (taskState.isLoading) {
      return Center(child: CircularProgressIndicator());
    }
    
    // Mostrar error
    if (taskState.error != null) {
      return ErrorWidget(
        message: taskState.error!,
        onRetry: () => ref.read(taskListProvider.notifier).loadTasks(),
      );
    }
    
    // Mostrar lista
    return ListView.builder(
      itemCount: taskState.tasks.length,
      itemBuilder: (context, index) {
        final task = taskState.tasks[index];
        return TaskCard(task: task);
      },
    );
  }
}

// Crear nueva tarea
void _onCreateTask() {
  ref.read(taskListProvider.notifier).createTask(
    name: 'Estudiar Flutter',
    durationSeconds: 1800, // 30 minutos
    color: '#3BCDFE',
  );
}
```

---

## 🔗 Referencias

- **TASK_BREAKDOWN.md#2.2.2**
- **Riverpod StateNotifier:** https://riverpod.dev/docs/providers/state_notifier_provider
- **Clean Architecture:** docs/ARCHITECTURE.md
- **Conventional Commits:** https://www.conventionalcommits.org/

---

## ➡️ Próximos Pasos

### Dependencias Futuras (Sprint 2)

Para completar la funcionalidad, se requiere:

1. **Tarea 3.1.1:** Crear entidad `Task` en `/domain/entities/`
2. **Tarea 3.1.3:** Crear interfaz `TaskRepository` en `/domain/repositories/`
3. **Tarea 3.1.4:** Implementar `TaskRepositoryImpl` en `/data/repositories/`
4. **Tarea 3.2.1-3.2.5:** Crear UseCases (CreateTask, GetAllTasks, etc.)

### TODOs Pendientes en el Código

```dart
// lib/presentation/providers/task_provider.dart

// TODO(sprint-2): Importar entidades cuando estén implementadas
// TODO(sprint-2): Inyectar TaskRepository
// TODO(sprint-2): Implementar con repository real
// TODO(sprint-2): Implementar validaciones
// TODO(sprint-2): Crear tarea con repository
// TODO(sprint-2): Inyectar dependencias
```

### Siguiente Tarea Inmediata

**Tarea 2.2.3:** Crear provider de temporizador (`timerStateProvider`)

---

## 🎯 Notas de Implementación

### Patrón Aplicado

Este provider sigue el patrón **Repository Pattern + Provider Pattern**:

1. **UI Layer** → Observa provider
2. **Provider Layer** → Gestiona estado + llama use cases
3. **Domain Layer** → Define reglas de negocio (futuro)
4. **Data Layer** → Accede a SQLite (futuro)

### Beneficios de Esta Estructura

✅ **Testeable:** Estado y lógica separados facilitan unit tests  
✅ **Reactivo:** Cambios en estado actualizan UI automáticamente  
✅ **Escalable:** Fácil agregar más métodos o estados  
✅ **Mantenible:** TODOs claros indican qué completar  
✅ **Type-safe:** Dart + Riverpod previenen errores en compilación  

---

**✨ Tarea completada exitosamente**
